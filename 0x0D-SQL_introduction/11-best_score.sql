-- shows score and name columns with higher scores from second_table
-- shows score and name columns higher score from second_table
SELECT score, name FROM second_table WHERE score >= 10 ORDER BY score DESC;
